树结构---数据库表设计：
# 领接表
只记录父子节点关系  

# 路径枚举:
通过字符串或数组记录当前节点到根节点的所有节点枚举



# 嵌套集，nsleft小于所以节点后代id，nsright大于所有节点后代id ，


# 闭包表:是一种用于存储和查询树形数据结构的技术
通过空间换时间，记录祖先和所有后代的关系,以及之间的距离层次distance
treepaths记录祖先和后代的所有关系